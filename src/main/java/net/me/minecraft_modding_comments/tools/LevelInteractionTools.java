package net.me.minecraft_modding_comments.tools;

import net.me.minecraft_modding_comments.tools.custom.Action;
import net.me.minecraft_modding_comments.tools.custom.ShapeCalculation;
import net.minecraft.world.level.Level;

import java.util.HashMap;

public class LevelInteractionTools {
    public static HashMap Sphere(ShapeCalculation shape, Level level, int radius) {
        if (shape.equals(ShapeCalculation.SQUARE)) {
            //for (int i = 1; i < )
        }
    }
}
